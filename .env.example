# Optional. With a Serper key (https://serper.dev, free tier available) StoreProof runs LIVE
# verification against real search results. WITHOUT it, the app runs in DEMO mode against a
# built-in sample catalogue so judges can try it with zero setup.
SERPER_API_KEY=
